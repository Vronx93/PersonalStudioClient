// import { render, screen } from "../../test/test-utils"
// import ShopItemList from "./ShopItemList"
// import { MemoryRouter } from "react-router-dom"
// import { mockItemList } from "../../test/mocks/mockData"

describe("ShopItemList component with items", () => {
  test("should render list with 2 list items", async () => {
    // render(<MemoryRouter><ShopItemList shopItems={mockItemList} /></MemoryRouter>)
    // const list = await screen.findByRole("list")
    // const listItems = await screen.findAllByRole("listitem")
    // expect(list).toBeVisible()
    // expect(listItems).toHaveLength(2)
  });
});
