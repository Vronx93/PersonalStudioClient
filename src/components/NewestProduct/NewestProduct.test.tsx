// import { MemoryRouter } from "react-router-dom"
// import { render, screen } from "../../test/test-utils"
// import NewestProduct from "./NewestProduct"

describe("NewestProduct component", () => {
  test("should render 4 images(item image, two waves images and background img), two headings, link", () => {
    // render(<MemoryRouter><NewestProduct product={undefined}/></MemoryRouter>)
    // const images = screen.getAllByRole("img")
    // const headings = screen.getAllByRole("heading")
    // const link = screen.getByRole("link")
    // expect(images).toHaveLength(4)
    // expect(headings).toHaveLength(2)
    // expect(link).toBeVisible()
  });
});
