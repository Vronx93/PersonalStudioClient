// import { render, screen } from "@testing-library/react"

describe("Login page", () => {
    // should render
    // should redirect to correct link on success
    // should display error message on incorrect inputs or on fail fetch
    test("", () => {

    })
})